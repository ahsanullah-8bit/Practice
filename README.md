# Practice
Some of my practice projects

# Build from source

# On Linux
You need these packages when using vcpkg

```bash
sudo apt install autoconf autoconf-archive automake libtool libx11-dev libxft-dev libxext-dev libxi-dev libxtst-dev libgles2-mesa-dev libxrandr-dev
```